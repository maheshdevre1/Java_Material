Que 1) Concurrent Hashmap internal working ?
---------------------------------------------
When we create an object of ConcurrentHashMap in Java, 16 buckets are not created immediately. 
The table is created lazily when the first entry is added (default capacity is 16).
When we add an entry:
First it calculates hashCode
Then it calculates index value using that hash
Based on index, entry is assigned to a particular bucket in Node format
If bucket is null:
Entry is inserted using CAS (no locking required)
If bucket is not null:
It will lock only that particular bucket (not whole map)
Then it checks:
If key already exists → value is updated
If key not exists → entry is added in linked list
After operation completes:
Lock is automatically released (no manual unlock)
If more than 8 entries are present in the same bucket:
Linked list is converted into tree structure (Red-Black Tree)
(only if table size ≥ 64, otherwise resizing happens)

Que 2) Difference between PutMapping and PatchMapping ?
---------------------------------------------------------
@PutMapping	 - Update entire resource
@PatchMapping	- Update partial resource

Que 3) How to use Employee object as key in HashMap? 
----------------------------------------------------
In Java, if you want to use a custom object (like Employee) as a key in HashMap, you must override:
👉 hashCode()
👉 equals()
🔹 Why Required?
HashMap internally uses:
hashCode() → to find bucket
equals() → to compare keys inside bucket
❗ If you don’t override:
Duplicate keys may be inserted
Retrieval may fail ==> We will get null values 

Que 4) Diff between @Primary and @Component ?
---------------------------------------------
When you have multiple beans of same type, Spring gets confused:
@Component
class Car {}
@Component
class Bike {}
@Autowired
Vehicle vehicle; // ❌ Error: multiple beans
👉 This is called NoUniqueBeanDefinitionException
🔹 2. @Primary
👉 Default bean choose karne ke liye
@Component
@Primary
class Car implements Vehicle {}
@Component
class Bike implements Vehicle {}
@Autowired
Vehicle vehicle;
👉 Output:
Car bean injected ✅
✔ Spring automatically selects @Primary bean
🔹 3. @Qualifier
👉 Specific bean choose karne ke liye
@Component("carBean")
class Car implements Vehicle {}
@Component("bikeBean")
class Bike implements Vehicle {}
@Autowired
@Qualifier("bikeBean")
Vehicle vehicle;
👉 Output:
Bike bean injected ✅
✔ You explicitly tell Spring which bean to use
🔥 Key Differences
Feature	@Primary	@Qualifier
Purpose	Default selection	Specific selection
Usage	Class level	Field/parameter level
Control	Less	More precise
When used	When 1 bean should be default	When multiple choices needed
